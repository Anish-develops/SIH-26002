# =============================================================================
# NER Sentinel — Environment Configuration
# SIH Problem Statement 26002 | Decision-Support Platform for NER
# =============================================================================

# Prototype Environment Flag
NODE_ENV=development
ENVIRONMENT=demo
DATA_SOURCE=synthetic

# Backend Service
PORT=4000
API_URL=http://localhost:4000

# Web Command Center
VITE_API_URL=http://localhost:4000

# Driver Mobile App
EXPO_PUBLIC_API_URL=http://localhost:4000

# -----------------------------------------------------------------------------
# Future Production PostgreSQL / PostGIS Integration
# (Turnkey ready: see services/api/src/database/schema.sql)
# -----------------------------------------------------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ner_sentinel
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
